(function() {
  /* Options
  accent: {
    list of <key,value> pair where key is replaced with value
    this is case-sensitive
  }
  */
  
  function init(opt, slideshow) {
  }
  
  remark.register(init);
})();